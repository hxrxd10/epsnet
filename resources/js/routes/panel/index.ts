import bandeja from './bandeja'
import estudiantes from './estudiantes'

const panel = {
    bandeja: Object.assign(bandeja, bandeja),
    estudiantes: Object.assign(estudiantes, estudiantes),
}

export default panel